[package]
name = "focaldesk-memory"
version = "0.1.0"
edition = "2021"
description = "Core primitives, traits, and shared types for FocalDesk"
license = "MIT"
authors = ["Steve"]

[lib]
name = "focaldesk_memory"
path = "src/lib.rs"

[dependencies]
