[package]
name = "focaldesk-memory"
version = "0.1.0"
edition = "2021"
description = "Vector storage, embedding generation, semantic search, and long-term memory support for FocalDesk AI"
license = "MIT"
authors = ["Steve"]

[lib]
name = "focaldesk_memory"
path = "src/lib.rs"

[dependencies]
