pub struct RenderResources {
    // texture atlas
    // svg cache
    // text resources
}

impl RenderResources {
    pub fn new() -> Self {
        Self {
            // initialize caches
        }
    }
}
