[package]
name = "flowstate-ipc"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
flowstate-settings-core = { path = "../flowstate-settings-core" }
flowstate-logging = { path = "../flowstate-logging" }
