#%PAM-1.0
#
# Human login policy for focaldmd on Fedora.
# Focaldesk's PAM module unlocks focald-secrets before the desktop session.
# Do not start GNOME Keyring here: focald-secrets owns the standard
# org.freedesktop.secrets API used by Chrome and other Secret Service clients.

auth        [success=done ignore=ignore default=bad] pam_selinux_permit.so
auth        substack      password-auth
auth        optional      pam_focald_secrets.so
auth        include       postlogin

account     required      pam_nologin.so
account     include       password-auth

password    substack      password-auth
password    optional      pam_focald_secrets.so

session     required      pam_selinux.so close
session     required      pam_loginuid.so
session     required      pam_selinux.so open
session     optional      pam_keyinit.so force revoke
session     required      pam_namespace.so
session     include       password-auth
session     optional      pam_focald_secrets.so
session     include       postlogin
