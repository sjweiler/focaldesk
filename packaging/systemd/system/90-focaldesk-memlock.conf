[Service]
# The per-user manager must be allowed to pass focald-secrets its 128 MiB
# LimitMEMLOCK. Fedora's inherited 8 MiB hard limit is too small for
# mlockall(MCL_CURRENT|MCL_FUTURE).
LimitMEMLOCK=128M
